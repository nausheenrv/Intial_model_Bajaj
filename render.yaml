services:
  - type: web
    name: hackrx-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn extract:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
    plan: free
